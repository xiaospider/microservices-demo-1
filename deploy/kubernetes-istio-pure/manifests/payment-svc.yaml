---
apiVersion: v1
kind: Service
metadata:
  name: payment
  labels:
    app: payment
    service: payment
  namespace: sock-shop-pure
spec:
  ports:
    # the port that this service should serve on
  - port: 80
    name: http
    targetPort: 80
  selector:
    app: payment
